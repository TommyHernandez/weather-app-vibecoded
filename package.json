{
  "name": "02-weather",
  "description": "A simple terminal app to know the weather in a concrete city",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build index.ts --outdir dist --target=bun",
    "start": "bun index.ts",
    "dev": "bun index.ts",
    "dev:hot": "bun --hot index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
